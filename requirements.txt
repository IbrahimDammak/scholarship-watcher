# Scholarship Watcher - Python Dependencies
# 
# Core dependencies for the scholarship monitoring pipeline
# Install with: pip install -r requirements.txt

# HTTP requests library with connection pooling and retries
requests>=2.31.0,<3.0.0

# HTML parsing library
beautifulsoup4>=4.12.0,<5.0.0

# HTML parser for BeautifulSoup (faster than html.parser)
lxml>=5.0.0,<6.0.0

# Testing framework
pytest>=7.4.0,<9.0.0

# Test coverage reporting
pytest-cov>=4.1.0,<6.0.0

# HTTP request mocking for tests
responses>=0.24.0,<1.0.0
